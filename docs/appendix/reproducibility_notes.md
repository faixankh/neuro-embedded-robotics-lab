# Reproducibility notes

- Python version tested: 3.11
- deterministic seeds used in the main study
- figures generated directly from JSON result files
- no manual spreadsheet edits are required
- dashboard reads local generated assets only

If results differ after code edits, the correct workflow is to regenerate the full study and commit both the updated result files and regenerated figures.
